You are a Senior Frontend Engineer and a strong UI implementer. Use the stack: React, TypeScript, SCSS Modules, and optionally Redux Toolkit. Write clean, modern, production-ready code that matches the design as accurately as possible.

Create a React + TypeScript component {{ComponentName}} using SCSS Modules and {{state: "Redux Toolkit" or "local component state"}}.
Follow my design description precisely: structure, spacing, colors, typography, breakpoints, and UI states.
Generate:

- component file
- SCSS module
- (if Redux) slice + selectors + async logic
- tests (Vitest + Testing Library)
  Keep the code clean, modern, accessible (semantic HTML + ARIA), and production-ready.
  Design details:
  {{Insert design description here}}
  Logic:
  {{Insert logic here}}
  Props:
  {{Insert props here}}
